<link rel="stylesheet" href="/css/pizzeria_menu.css">

<div class="background_image_pizzeria_menu">
<nav>
    <div class="pizzeria_menu_btn">
        <a href="/admin/pizzeria_menu"><button>Pizzas Salgadas</button></a>
    </div>
    <div class="pizzeria_menu_btn">
        <a href="/admin/sweet_pizza"><button>Pizzas Doces</button></a>
    </div>
    <div class="pizzeria_menu_btn">
        <a href="/admin/drink_menu"><button>Bebidas</button></a>
    </div>
    <div class="pizzeria_menu_btn">
        <a href="/admin/several_menu"><button>Diversos</button></a>
    </div>
</nav>

{{#each pizza}}
<div class="pizzeria_menu">
    <h1>{{flavor}}</h1>
    <h3 class="description_pizzeria_menu">{{description}}</h3>
    <br>
    <div class="prices">
    <p>Pequena - {{price_small}}</p>
    <p>Média - {{price_average}}</p>
    <p>Grande - {{price_big}}</p>
    </div>
    <a href="/admin/menu/remove/{{_id}}"><button class="request_red_button" >Deletar</button></a>
    
</div>

{{else}}

{{/each}}

{{#each several}}

<div class="pizzeria_menu">
    <h1>{{name}}</h1>
    <h3 class="description_pizzeria_menu">{{description}}</h3>
    <br>
    <div class="prices">
    <p>{{price}}</p>
    </div>
    <a href="/admin/several/remove/{{_id}}"><button class="request_red_button" >Deletar</button></a>
</div>

{{else}}

{{/each}}
</div>